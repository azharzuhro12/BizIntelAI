{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "fastapi",
  "fluid": true,
  "regions": ["sin1"],
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "excludeFiles": "{frontend/**,notebooks/**,docs/**,scripts/**,data/processed/**,data/ml_outputs/**,data/evaluation/**,backend/tests/**,*.md,docker-compose.yml}"
    }
  }
}
